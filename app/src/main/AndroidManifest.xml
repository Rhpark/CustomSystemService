<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions for alarm functionality -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    
    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SystemService"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alarm Test Activity -->
        <activity
            android:name=".AlarmTestActivity"
            android:exported="false"
            android:label="Alarm Controller Test"
            android:parentActivityName=".MainActivity" />

        <!-- Alarm Test Activity -->
        <activity
            android:name=".NotificationTestActivity"
            android:exported="false"
            android:label="Notification Controller Test"
            android:parentActivityName=".MainActivity" />

        <!-- Test Alarm Receiver -->
        <receiver
            android:name=".TestAlarmReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>


        <!-- SoftKeyboard Test Activity -->
        <activity
            android:name=".SoftKeyboardTestActivity"
            android:exported="false"
            android:label="SoftKeyboard Controller Test"
            android:parentActivityName=".MainActivity" />

        <!-- Vibrator Test Activity -->
        <activity
            android:name=".VibratorTestActivity"
            android:exported="false"
            android:label="Vibrator Controller Test"
            android:parentActivityName=".MainActivity" />

        <!-- FloatingView Test Activity -->
        <activity
            android:name=".FloatingViewTestActivity"
            android:exported="false"
            android:label="FloatingView Controller Test"
            android:parentActivityName=".MainActivity" />

        <!-- Battery Test Activity -->
        <activity
            android:name=".BatteryTestActivity"
            android:exported="false"
            android:label="Battery State Monitor Test"
            android:parentActivityName=".MainActivity" />

        <!-- Display Test Activity -->
        <activity
            android:name=".DisplayTestActivity"
            android:exported="false"
            android:label="Display Information Test"
            android:parentActivityName=".MainActivity" />

        <!-- Location Test Activity -->
        <activity
            android:name=".LocationTestActivity"
            android:exported="false"
            android:label="Location Service Test"
            android:parentActivityName=".MainActivity" />

        <!-- Display Test Activity -->
        <activity
            android:name=".DisplayTestActivity"
            android:exported="false"
            android:label="Display Test"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>